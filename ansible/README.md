# Ansible

Este diretório concentra os playbooks, inventários e roles usados para configurar as máquinas do laboratório.

## Estrutura sugerida

- `ansible.cfg` para configuração padrão do Ansible
- `inventories/` para inventários por ambiente
- `playbooks/` para playbooks executáveis
- `roles/` para lógica reutilizável